<!DOCTYPE html>
<html>
<head>
	<title>试卷</title>
	<meta charset="utf-8" />
	<meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<meta name="apple-mobile-web-app-capable" content="yes">
	<meta HTTP-EQUIV="pragma" CONTENT="no-cache">
	<meta HTTP-EQUIV="Cache-Control" CONTENT="no-cache, must-revalidate">
	<meta HTTP-EQUIV="expires" CONTENT="0">
	<style type="text/css">
	h3{white-space:pre-wrap}
	</style>
</head>
<body>
<center>
<h1>P0046-网站移动开发理论1</h1>
<h3>
学院:_______________ 班级:_______________ 姓名:_______________ 得分:_______________   总分:100分  
</h3>
</center>
<div id="exampaper-body" style="padding: 0px">
	<!-- 开始加载试卷 -->
			单选题  每道题（3分）
				<h3><pre>1、下列选项中哪个符合标识符规范？</pre></h3>
					<p>A:Function</p>
					<p>B:typeof</p>
					<p>C:_function</p>
					<p>D:function</p>
			
				<h3><pre>2、下列选项中哪个运行会报错？</pre></h3>
					<p>A:var s = new String('s');</p>
					<p>B:var u = new Undefined();</p>
					<p>C:var bool = new Boolean(false)</p>
					<p>D:var num = new Number(0);</p>
			
				<h3><pre>3、执行var a=[0, 1, 2];console.log(a.join());将会打印：</pre></h3>
					<p>A:12</p>
					<p>B:0 1 2</p>
					<p>C:0,1,2</p>
					<p>D:[0,1,2]</p>
			
				<h3><pre>4、下列布尔表达式返回true的是：</pre></h3>
					<p>A:isNaN('abc') == NaN</p>
					<p>B:typeof NaN === 'number'</p>
					<p>C:parseInt(2.5) == parseFloat(2.5)</p>
					<p>D:parseInt(12.5) == parseFloat(12.5)</p>
			
				<h3><pre>5、执行var  s=&quot;abcdefg&quot;;alert(s.substring(1,3));</pre></h3>
					<p>A:ab</p>
					<p>B:abc</p>
					<p>C:bc</p>
					<p>D:bcd</p>
			
				<h3><pre>6、下面数组方法中，会直接影响原数组的是：</pre></h3>
					<p>A:task</p>
					<p>B:index</p>
					<p>C:app</p>
					<p>D:default</p>
			
				<h3><pre>7、下列选项中，至今没有组织对其进行规范的是</pre></h3>
					<p>A:DOM</p>
					<p>B:CSS</p>
					<p>C:ECMAScript</p>
					<p>D:BOM</p>
			
				<h3><pre>8、下列选项中不属于字符串变量方法的是</pre></h3>
					<p>A:indexOf()</p>
					<p>B:charAt()</p>
					<p>C:split()</p>
					<p>D:splice()</p>
			
				<h3><pre>9、（网站）align-self属性值不包含？</pre></h3>
					<p>A:flex</p>
					<p>B:flex-start</p>
					<p>C:flex-end</p>
					<p>D:center</p>
			
				<h3><pre>10、（网站）所有网站内容在一屏高度显示，页面中不出现滚动条的页面形式叫做</pre></h3>
					<p>A:单屏网页</p>
					<p>B:双飞布局</p>
					<p>C:移动应用</p>
					<p>D:双屏显示</p>
			
				<h3><pre>11、（网站）以下宽度在500px到780px的写法正确的是？</pre></h3>
					<p>A:@media screen and (min-width:500px) (max-width:780px）</p>
					<p>B:@media (max-width:500px) and (min-width:780px)</p>
					<p>C:@media screen (max-width:500px) and (min-width:780px)</p>
					<p>D:@media screen and (min-width:500px) and (max-width:780px）</p>
			
				<h3><pre>12、（网站）下面Media Queries的使用语法正确的是？</pre></h3>
					<p>A:@media and 设备类型 (设备特性) { 样式代码 }</p>
					<p>B:@media设备类型and(设备特性){ 样式代码 }</p>
					<p>C:@media 设备类型 and (设备特性) { 样式代码 }</p>
					<p>D:@media设备类型and(设备特性){ 样式代码 }</p>
			
				<h3><pre>13、（网站）通过（）样式可以去除系统默认的样式，一般用于解决input元素更改完样式后导致无法使用的兼容问题？</pre></h3>
					<p>A:cursor : none;</p>
					<p>B:appearance : none;</p>
					<p>C:outline :0;</p>
					<p>D:nav-up : none;</p>
			
				<h3><pre>14、（网站）一个网站能够兼容多个终端而不是为每一个终端做一个特定的版本的布局方式是（）？</pre></h3>
					<p>A:弹性盒布局</p>
					<p>B:双飞翼布局</p>
					<p>C:圣杯布局</p>
					<p>D:响应式布局</p>
			
				<h3><pre>15、（网站）text-overflow设置为（    ）时表示当对象内文本溢出时显示省略标记？</pre></h3>
					<p>A:ellipsis</p>
					<p>B:clip</p>
					<p>C:none</p>
					<p>D:以上都不可以</p>
			
				<h3><pre>16、（网站）meta标签哪个属性设置设备宽度？</pre></h3>
					<p>A:width</p>
					<p>B:max-width</p>
					<p>C:device-width</p>
					<p>D:user-select</p>
			多选题  每道题（3分）
				<h3><pre>17、Math.ceil(-5.02)的结果是（），Math.floor(-5.02) 的结果是（）</pre></h3>
					<p>A:-5</p>
					<p>B:-5.02</p>
					<p>C:-6</p>
					<p>D:5.02</p>
			
				<h3><pre>18、下列数组方法中属于栈方法的是</pre></h3>
					<p>A:shift()</p>
					<p>B:pop()</p>
					<p>C:unshift()</p>
					<p>D:push()</p>
			
				<h3><pre>19、（网站）设为 Flex 布局以后，子元素的（）、（）和（）属性将失效？</pre></h3>
					<p>A:float</p>
					<p>B:clear</p>
					<p>C:vertical-align</p>
					<p>D:padding</p>
			
				<h3><pre>20、（网站）下列哪个选项设置在父元素上</pre></h3>
					<p>A:flex-wrap</p>
					<p>B:flex-shrink</p>
					<p>C:flex-grow</p>
					<p>D:justify-content</p>
			判断题  每道题（2分）
				<h3><pre>21、JS是弱类型语言，所以它没有数据类型？</pre></h3>
				<p>A:正确</p>
				<p>B:错误</p>
			
				<h3><pre>22、逻辑表达式 NaN == NaN 返回 false？</pre></h3>
				<p>A:正确</p>
				<p>B:错误</p>
			
				<h3><pre>23、已知num=0,执行n=++(--num+1) 后,n的值是1? </pre></h3>
				<p>A:正确</p>
				<p>B:错误</p>
			
				<h3><pre>24、函数定义时，通过arguments访问到的是实参？</pre></h3>
				<p>A:正确</p>
				<p>B:错误</p>
			
				<h3><pre>25、执行var falseObj = new Boolean(false);console.log(falseObj&amp;&amp;true);控制台将会打印true。</pre></h3>
				<p>A:正确</p>
				<p>B:错误</p>
			
				<h3><pre>26、（网站）通过+选择器来查找某个元素后一个兄弟元素.</pre></h3>
				<p>A:正确</p>
				<p>B:错误</p>
			
				<h3><pre>27、（网站）CSS3中的Media Query（媒体查询）它面对不同分辨率设备不够灵活。</pre></h3>
				<p>A:正确</p>
				<p>B:错误</p>
			
				<h3><pre>28、（网站）我们写的1px，在移动端，是逻辑像素的1px，而非物理像素的1px</pre></h3>
				<p>A:正确</p>
				<p>B:错误</p>
			
				<h3><pre>29、（网站）响应式布局主要使用js来实现。</pre></h3>
				<p>A:正确</p>
				<p>B:错误</p>
			
				<h3><pre>30、（网站）当子元素宽度超过父元素宽度时自动像内挤压，如果要保持子元素本身的宽度应该设置flex-shrink属性为0.</pre></h3>
				<p>A:正确</p>
				<p>B:错误</p>
			填空题  每道题（2分）
				<h3><pre>31、执行var s=&quot;Hello&quot;;s.toUpperCase();console.log(s);终端打印的信息是：</pre></h3>
			
			
				<h3><pre>32、执行var a=[];var b=[];console.log(a==b)将打印：_</pre></h3>
			
			
				<h3><pre>33、执行var a = true &amp;&amp; 0 &amp;&amp; &quot;1&quot;;console.log(a);将打印：_</pre></h3>
			
			
				<h3><pre>34、JS标识符必须以哪三种字符开头？</pre></h3>
			
			
				<h3><pre>35、函数定义有哪两种方式？</pre></h3>
			
			
				<h3><pre>36、（网站）align-content属性设为（   ）时表示各行在弹性盒容器中平均分布，两端保留子元素与子元素之间间距大小的一半？</pre></h3>
			
			
				<h3><pre>37、（网站）（）用来设置垂直方向上的对齐方式？</pre></h3>
			
			
				<h3><pre>38、（网站）order属性定义项目的排列顺序。数值越（），排列越靠后</pre></h3>
			
			
				<h3><pre>39、（网站）backface-visibility 属性定义当元素不面向屏幕时是否可见，当它的值为（   ）时表示背面是不可见的？</pre></h3>
			
			
				<h3><pre>40、（网站）强制让文字在一行显示可通过（）实现？</pre></h3>
			
</div>
</body>
</html>
